CREATE TABLE entrenamiento (
                               id INT AUTO_INCREMENT PRIMARY KEY,
                               tipo VARCHAR(50),
                               ubicacion VARCHAR(100),
                               fecha DATE
);
/**Tabla entrenamiento que creamos porque nos lo pide el ejercicio...